# Expose only non-secret resource IDs/endpoints, such as server/database IDs and the server hostname, when implemented.
# Never expose secrets, access keys, tokens, passwords, or secret-bearing connection strings.
