# Intended resource boundary: PostgreSQL flexible server/database configuration, network integration, and extension support where applicable.
# SQL schema migrations, pgvector indexes, embeddings, and query performance testing remain SQL/application work.
# No resources or module calls are implemented.
# Verify planned features and current provider support, including extension configuration boundaries, when implementing.
