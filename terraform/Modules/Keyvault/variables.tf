# Define typed, documented inputs for vault configuration and least-privilege access later.
# Use existing resource IDs where appropriate, including network, identity, and access-scope references.
# Avoid secret-value inputs; Terraform-managed secrets can be present in state.
