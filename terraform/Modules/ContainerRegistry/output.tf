# Expose only non-secret resource IDs/endpoints, such as the registry ID and login endpoint, when implemented.
# Never expose secrets, access keys, tokens, passwords, or secret-bearing connection strings.
