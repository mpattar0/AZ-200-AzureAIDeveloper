# Expose only non-secret resource IDs/endpoints, such as the cluster ID and API endpoint, when implemented.
# Never expose secrets, access keys, tokens, passwords, kubeconfig credentials, or secret-bearing connection strings.
