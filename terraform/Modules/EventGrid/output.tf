# Expose only non-secret resource IDs/endpoints for the namespace, topics, and event subscriptions when implemented.
# Never expose secrets, access keys, tokens, passwords, or secret-bearing connection strings.
