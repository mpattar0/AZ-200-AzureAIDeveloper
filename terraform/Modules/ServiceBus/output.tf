# Expose only non-secret resource IDs/endpoints for the namespace and messaging entities when implemented.
# Never expose secrets, access keys, tokens, passwords, or secret-bearing connection strings.
