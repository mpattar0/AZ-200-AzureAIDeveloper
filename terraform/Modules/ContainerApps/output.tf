# Expose only non-secret resource IDs/endpoints, such as environment/app IDs and ingress hostnames, when implemented.
# Never expose secrets, access keys, tokens, passwords, or secret-bearing connection strings.
