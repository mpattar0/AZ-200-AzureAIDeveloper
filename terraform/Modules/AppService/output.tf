# Expose only non-secret resource IDs/endpoints, such as plan/app IDs and app hostnames, when implemented.
# Never expose secrets, access keys, tokens, passwords, or secret-bearing connection strings.
