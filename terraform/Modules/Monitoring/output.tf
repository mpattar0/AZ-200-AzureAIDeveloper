# Expose only non-secret resource IDs/endpoints for the workspace and Application Insights when implemented.
# Never expose secrets, access keys, tokens, passwords, or secret-bearing connection strings.
