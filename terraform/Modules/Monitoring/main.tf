# Intended resource boundary: Log Analytics workspace and Application Insights together, plus query alert rules as appropriate.
# Actual OpenTelemetry SDK instrumentation and KQL exercises remain application/exercise work.
# Shared monitoring references are consumed by other service modules rather than recreated there.
# No resources or module calls are implemented.
# Verify planned features and current provider support when implementing.
