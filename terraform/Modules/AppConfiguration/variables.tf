# Define typed, documented inputs for the store, non-secret key-values, and Key Vault references later.
# Use existing resource IDs where appropriate, including vault, identity, and network references; do not accept secret values.
