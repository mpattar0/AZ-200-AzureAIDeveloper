# Expose only non-secret resource IDs/endpoints, including identity, principal, and client IDs, when implemented.
# Never expose secrets, access keys, tokens, passwords, or secret-bearing connection strings.
