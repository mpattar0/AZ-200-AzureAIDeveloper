# Intended resource boundary: user-assigned managed identities and narrowly scoped role assignments where required.
# Root/provider configuration supplies credentials; system-assigned identities belong to their owning service modules.
# Agree role-assignment ownership with consuming modules to avoid duplicate grants.
# No resources or module calls are implemented.
# Verify planned features and current provider support when implementing.
