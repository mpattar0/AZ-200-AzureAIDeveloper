# Expose only non-secret resource IDs/endpoints, such as the function app ID and hostname, when implemented.
# Never expose secrets, access keys, function keys, tokens, passwords, or secret-bearing connection strings.
